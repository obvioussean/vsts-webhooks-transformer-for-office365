// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "scripts/*.js": {
            "when": "$(basename).ts"
        },
        "scripts/*.map": true,
        "node_modules": true,
        "*.gitignore": true
    }
}